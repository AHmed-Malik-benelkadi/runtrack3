
var count = 0; // Initialisation du compteur
function addone() {
    count++; // Incrémentation du compteur
    var compteur = document.getElementById("compteur");
    compteur.innerHTML = count; // Mise à jour du contenu du paragraphe
}
